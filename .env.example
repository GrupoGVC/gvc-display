DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=db_gvc_display
DB_USER=root
DB_PASS=

JWT_SECRET=insira_aqui
JWT_TTL=86400

# APP_BASE_PATH é opcional — se vazio, detecta automaticamente pelo SCRIPT_NAME
# Descomente e preencha APENAS se o auto-detect não funcionar:
APP_BASE_PATH=/gvc-display
